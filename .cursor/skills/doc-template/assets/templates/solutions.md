---
category: <bugs | architecture | design-patterns | conventions | workflow>
tags: [<tag>, <tag>]
problem-type: <silent-failure | drift | mis-scoped-rule | …>
status: <draft | active | superseded>
---

# `<Lesson Title>`

[Authoring notes — delete this block before saving.
PURPOSE: improve's durable learning — the caught lesson plus the thin pack rule it spawned, so the next cycle reads
a sharper convention. The companion `.cursor/rules/<name>.mdc` is written separately; this doc is the human-facing
record of why it exists.
DATE goes in the entry below (the **Date** line), not the filename — the slug names the file.
CITE, DON'T RESTATE the `.ai/` source — point at it; the rule's invariant must be verified TRUE against the live
tree before this is written. Keep no parallel convention prose that would drift from `.ai/`.
FLOOR (always): Date · Lesson · The rule it spawned.]

**Date:** YYYY-MM-DD

## Lesson
[What was caught and why it matters, in 1-3 lines — the convention that wasn't written down.]

## The rule it spawned
[The thin rule codified: type · glob · `.ai/` source pointer · the falsifiable invariant + the check it ties to.
Name the `.cursor/rules/<name>.mdc` written, and the upstream `.ai/` proposal staged (if any).]

## Basis
[Include when it aids the next reader — where the lesson came from (the originating `CANDIDATE-RULE` report or the
human correction) and the `file:line` the invariant was verified against.]
