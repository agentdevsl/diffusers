# DiffUser implement & ship

## Sequential implement-ship workflow

Run this implement-ship workflow on the current change, invoking each step in order — finish each before starting the next:

* /implement
* /diffu-review
* /ship
